checkout:
  post:
    - git submodule update --init --recursive

dependencies:
  cache_directories:
    - kanjivg

general:
  artifacts:
    - converted

deployment:
  master:
    branch: master
    commands:
      - python kanimaji.py
